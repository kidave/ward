/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'gostxgfnoilfmybaohhx.supabase.co',
      // Add other domains if needed
    ],
  },
  // ... other config options
}

module.exports = nextConfig